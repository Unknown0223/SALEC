/**
 * Klient Excel importi uchun maksimal fayl hajmi (bayt).
 * Backend `MULTIPART_MAX_FILE_BYTES` bilan mos kelishi kerak (standart 50 MB).
 */
export const CLIENT_IMPORT_MAX_FILE_BYTES = 50 * 1024 * 1024;
