/**
 * Maksimal jami dona (oplata + bonus — ikkalasi ham omborga) bitta qaytarish hujjatida.
 * Backend: `MAX_RETURN_ITEMS` (`returns-enhanced.service.ts`) bilan bir xil bo‘lishi kerak.
 */
export const MAX_RETURN_PHYSICAL_UNITS_PER_DOCUMENT = 24;
