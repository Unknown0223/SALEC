-- Sum-rule threshold: compare min_sum to current order only, or to calendar-month cumulative merchandise (Asia/Tashkent month boundaries in app).
ALTER TABLE "bonus_rules" ADD COLUMN "sum_threshold_scope" TEXT NOT NULL DEFAULT 'order';
