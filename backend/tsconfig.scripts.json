{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "rootDir": "..",
    "noEmit": true
  },
  "include": ["scripts/**/*.ts", "../shared/**/*.ts"],
  "exclude": ["tests", "dist", "node_modules", "scripts/**/*.spec.ts"]
}
