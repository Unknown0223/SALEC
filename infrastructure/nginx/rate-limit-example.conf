# Nginx misoli: API oldida qo‘shimcha cheklov (Fastify login rate-limit bilan birga).
# `http` blokiga `limit_req_zone` qo‘shing, `server` / `location` da `limit_req` ishlating.

# http { ... ichida:
# limit_req_zone $binary_remote_addr zone=api_login:10m rate=5r/m;
# limit_req_zone $binary_remote_addr zone=api_general:10m rate=30r/s;

# location = /auth/login {
#   limit_req zone=api_login burst=10 nodelay;
#   proxy_pass http://backend_upstream;
# }
#
# location /api/ {
#   limit_req zone=api_general burst=50 nodelay;
#   proxy_pass http://backend_upstream;
# }

# Batafsil: nginx.org/rate-limiting.html
