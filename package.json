{
  "name": "salesdoc",
  "private": true,
  "description": "Monorepo: bitta buyruqda API + web panel",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "ports:free": "npx kill-port 4000 3000 3001 3002 3003",
    "repair:next": "npx kill-port 3000 3001 3002 3003 && npm run clean --prefix frontend",
    "open:panel": "start http://127.0.0.1:3000",
    "dev": "npx kill-port 4000 3000 && concurrently -n api,web -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:clean": "npm run ports:free && npm run clean --prefix frontend && npm run dev",
    "dev:alt": "concurrently -n api,web -c cyan,magenta \"cross-env PORT=4001 npm run dev --prefix backend\" \"npm run dev --prefix frontend -- -p 3010\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "db:deploy": "node scripts/run-backend-npm.cjs db:deploy",
    "db:seed": "node scripts/run-backend-npm.cjs db:seed",
    "import:refs": "node scripts/run-backend-npm.cjs import:refs",
    "import:staff-csv": "node scripts/run-backend-npm.cjs import:staff-csv",
    "ensure:demo-logins": "node scripts/run-backend-npm.cjs ensure:demo-logins",
    "import:once": "node scripts/run-backend-npm.cjs import:once",
    "test": "npm run test:ci --prefix backend && npm run test --prefix frontend",
    "frontend:lint": "npm run lint --prefix frontend",
    "frontend:typecheck": "npm run typecheck --prefix frontend",
    "frontend:build": "npm run build --prefix frontend",
    "load:smoke": "node scripts/load-smoke.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "kill-port": "^2.0.1"
  }
}
